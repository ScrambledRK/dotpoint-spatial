package at.dotpoint.display.renderable;

import at.dotpoint.datastructure.entity.event.SignalType;

/**
 *
 */
class RenderableSignal
{
    public static var DEPTH_CHANGED(default,never):SignalType = "Renderable.DEPTH";
}
